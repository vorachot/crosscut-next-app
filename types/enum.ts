export enum Status {
  ready = "AVAILABLE",
  cancelled = "CANCELLED",
  redeemed = "RUNNING",
  succeeded = "SUCCEEDED",
  inactive = "STOPPED",
}
export enum ResourceType {
  cpu = "CPU",
  gpu = "GPU",
  memory = "RAM",
}

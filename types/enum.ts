export enum Status {
  available = "AVAILABLE",
  running = "RUNNING",
  succeeded = "SUCCEEDED",
  stopped = "STOPPED",
}
export enum ResourceType {
  cpu = "CPU",
  gpu = "GPU",
  memory = "MEMORY",
}
